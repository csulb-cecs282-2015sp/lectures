// "global" is not usually used to describe functions, but TECHNICALLY all 
// functions that we've made so far are "global" -- they are not in a namespace
// or in a class, so they are globally accessible.

void FunctionC();